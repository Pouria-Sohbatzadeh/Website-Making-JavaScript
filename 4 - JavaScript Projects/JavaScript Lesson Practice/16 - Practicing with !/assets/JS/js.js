const number_1 = 30;

const number_2 = 40;

if(!number_1 + number_2 !== 70) {
    console.log(`${number_1} + ${number_2} = 70`)
    console.log(!number_1)
}

// The ! operator turns the data type of ANY value, stored value to Boolean as well, And its value by default will
// be false.