const young_adult = 20

if(typeof young_adult === typeof 20){
    // You can replace young_adult with whatever valuesm variables, operator or functions you want.
    console.log(`This is what I needed`)
}

console.log(`
`)
// Jus to seperate the codes, Don't worry about this

// as for the else if(){} varient of the condition
const greeting = `Hello to you`

if (typeof greeting === typeof 20) {
    console.log(`I need text`)
}
else if(typeof greeting === typeof `text`) {
    console.log(`Now I understand WHAT you're saying`)
}